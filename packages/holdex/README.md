# @cynic/holdex

> HolDex integration for CYNIC - K-Score token quality analysis

**Category**: library | **Version**: 0.1.0 | **Quality**: 🟠 needs tests

## Installation

```bash
npm install @cynic/holdex
```

## Quick Start

```javascript
import { createHolDexClient } from '@cynic/holdex';

const instance = createHolDexClient();
```

## API Reference

### Classes

| Class | Description |
|-------|-------------|
| `HolDexClient` | HolDexClient implementation |

### Factory Functions

| Function | Description |
|----------|-------------|
| `createHolDexClient()` | Create HolDexClient instance |

### Singletons

| Function | Description |
|----------|-------------|
| `getKScoreTier()` | Get KScoreTier singleton |
| `getEScoreTier()` | Get EScoreTier singleton |

### Constants

`PHI_POWERS`, `K_SCORE`, `K_SCORE_TIERS`, `E_SCORE_MULTIPLIERS`, `E_SCORE_TIERS`, `E_SCORE_PHI_UNIT`, `MAX_DISCOUNT_CAP`, `FEE_RATIOS`, `REWARD_SPLITS`

### Functions

`calculateKScore`, `isHealthyKScore`, `isExceptionalKScore`, `calculateEScoreDiscount`, `distributeFee`, `judgeToken`, `batchJudgeTokens`, `calculateFinalScore`, `validateKScoreComponents`, `generateTokenReport`

## Dependencies

**CYNIC**: @cynic/core

## Stats

- **Source files**: 3
- **Test files**: 1
- **Test ratio**: 33%
- **Exports**: 23 named + default

---

*Auto-generated by CYNIC meta-cognition. "φ distrusts φ" - κυνικός*
