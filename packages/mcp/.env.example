# CYNIC MCP Server Environment Variables
#
# Copy this file to .env and fill in your values
#
# "φ distrusts φ" - κυνικός

# ==============================================================================
# MCP Server Configuration
# ==============================================================================

# Transport mode: 'stdio' (default) or 'http'
# - stdio: For Claude Desktop integration (JSON-RPC over stdin/stdout)
# - http: For remote deployment (Render, Docker, etc.)
MCP_MODE=stdio

# HTTP port (only used when MCP_MODE=http)
PORT=3000

# ==============================================================================
# Database Configuration
# ==============================================================================

# PostgreSQL connection string
# Required for full persistence (judgments, sessions, patterns, PoJ chain)
# Falls back to file-based or in-memory storage if not provided
# CYNIC_DATABASE_URL=postgresql://user:password@host:5432/database

# Redis connection string
# Optional: used for session caching and distributed locks
# CYNIC_REDIS_URL=redis://host:6379

# ==============================================================================
# File-based Fallback
# ==============================================================================

# Data directory for file-based persistence fallback
# Used when PostgreSQL is not available
# CYNIC_DATA_DIR=/path/to/data

# ==============================================================================
# Logging
# ==============================================================================

# Log level: 'debug', 'info', 'warn', 'error'
CYNIC_LOG_LEVEL=info

# ==============================================================================
# Integrator Service
# ==============================================================================

# Workspace root for cross-project integration
# Default: /workspaces
# INTEGRATOR_WORKSPACE_ROOT=/workspaces

# ==============================================================================
# Production Settings (Render, Docker)
# ==============================================================================

# Node environment
NODE_ENV=development

# ==============================================================================
# Docker Compose Configuration
# ==============================================================================

# Required for docker-compose up
POSTGRES_USER=cynic
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=cynic

# ==============================================================================
# Example Configurations
# ==============================================================================

# Local development (standalone):
# CYNIC_DATABASE_URL=postgresql://user:pass@localhost:5432/cynic
# CYNIC_REDIS_URL=redis://localhost:6379

# Local development (with Docker Compose):
# Uses CYNIC_DB_* env vars - see docker-compose.yml

# Render production:
# MCP_MODE=http
# PORT=3000
# CYNIC_DATABASE_URL=<from Render PostgreSQL service>
# CYNIC_REDIS_URL=<from Render Redis service>
# NODE_ENV=production
