# @cynic/identity

> CYNIC Identity Layer - Node identity, E-Score, and reputation management

**Category**: library | **Version**: 0.1.0 | **Quality**: 🔴 critical

## Installation

```bash
npm install @cynic/identity
```

## Quick Start

```javascript
import { createKeyManager } from '@cynic/identity';

const instance = createKeyManager();
```

## API Reference

### Classes

| Class | Description |
|-------|-------------|
| `KeyManager` | KeyManager implementation |
| `EScoreCalculator` | EScoreCalculator implementation |
| `WEIGHTS` | WEIGHTS implementation |
| `THRESHOLDS` | THRESHOLDS implementation |
| `EScore7DCalculator` | EScore7DCalculator implementation |
| `NodeIdentity` | NodeIdentity implementation |
| `IdentityStatus` | IdentityStatus implementation |
| `ReputationGraph` | ReputationGraph implementation |
| `TrustLevel` | TrustLevel implementation |

### Factory Functions

| Function | Description |
|----------|-------------|
| `createKeyManager()` | Create KeyManager instance |
| `createEScoreCalculator()` | Create EScoreCalculator instance |
| `createEScore7DCalculator()` | Create EScore7DCalculator instance |
| `createNodeIdentity()` | Create NodeIdentity instance |
| `createReputationGraph()` | Create ReputationGraph instance |

### Singletons

| Function | Description |
|----------|-------------|
| `getStatus()` | Get Status singleton |
| `getTrustLevel()` | Get TrustLevel singleton |

### Constants

`WEIGHTS`, `THRESHOLDS`, `TRUST_LEVELS`, `TOTAL_WEIGHT`, `PHI_POWERS`, `TRUST_DECAY_RATE`, `MAX_PROPAGATION_DEPTH`

### Functions

`generateKeypair`, `deriveNodeId`, `normalizeBurns`, `normalizeUptime`, `normalizeQuality`, `calculateEScore7D`, `toDbFormat`

## Dependencies

**CYNIC**: @cynic/core, @cynic/burns

## Stats

- **Source files**: 6
- **Test files**: 1
- **Test ratio**: 17%
- **Exports**: 28 named

---

*Auto-generated by CYNIC meta-cognition. "φ distrusts φ" - κυνικός*
